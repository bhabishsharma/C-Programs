#include<stdio.h>
int main()
{

    int a,b ;
    printf("enter two numbers");
    scanf("%d%d",&a,&b);
    if (a>b)
    {
        printf("a is lager number",a);
    }
    else
        printf("b is larger number",b);
    return 0;
}

